This folder is intentionally minimal now.

For the current PostgreSQL + pgvector setup, use [README_DOCKER_PG.md](../README_DOCKER_PG.md).

That document is the single source of truth for:

- the Docker Compose service
- the host port mapping (`55432`)
- the init script (`docker/initdb/init.sql`)
- the Python session test in `DBO/database.py`
